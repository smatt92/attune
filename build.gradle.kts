// Root build. Intentionally minimal: plugin versions are declared in settings.gradle.kts
// (pluginManagement.plugins) so they resolve lazily, per-module. Keep module-specific
// configuration in each module's own build.gradle.kts.
