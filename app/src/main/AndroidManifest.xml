<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Attune writes system settings via android.provider.Settings. The animation-scale tools
      (the performance persona) require WRITE_SECURE_SETTINGS, which a normal app cannot hold —
      it is granted once over adb:
          adb shell pm grant com.attune.app android.permission.WRITE_SECURE_SETTINGS
      font_scale instead uses the user-grantable WRITE_SETTINGS (Settings.System.canWrite()).
      Declaring both; onboarding (M3) explains the one-time grant and detects each state.
    -->
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Attune">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
